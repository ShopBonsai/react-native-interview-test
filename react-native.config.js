module.exports = {
  project: {
    ios: {},
    android: {}, // grouped into "project"
  },
  dependencies: {
    "local-rn-library": {
      root: "/root/libraries",
    },
  },
  assets: ["./assets/fonts/"], // stays the same
}
