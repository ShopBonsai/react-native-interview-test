import { StyleSheet } from "react-native"

export default StyleSheet.create({
  movieName: {
    fontSize: 12.5,
    fontWeight: "bold",
    letterSpacing: 0,
    textAlign: "left",
    color: "#000000",
  },
})
