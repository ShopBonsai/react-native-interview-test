import { StyleSheet } from "react-native"

export default StyleSheet.create({
  inventory: {
    marginTop: 3,
    marginBottom: 15,
    fontWeight: "bold",
    fontSize: 12.5,
    letterSpacing: 0,
    textAlign: "left",
    color: "#000000",
  },
})
