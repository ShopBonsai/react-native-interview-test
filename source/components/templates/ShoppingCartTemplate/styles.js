import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  contentWrapper: { flex: 1, paddingLeft: 20, paddingRight: 20 },
  shoppingCartListWrapper: { flex: 1 },
});

export { styles };
