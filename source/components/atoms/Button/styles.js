import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  buttonText: {
    textAlign: "center",
    letterSpacing: 1,
    fontWeight: "600",
    paddingTop: 13,
    paddingBottom: 13,
  },
});

export { styles };
