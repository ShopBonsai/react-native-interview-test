import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  contentContainer: {
    paddingTop: 25,
  },
  message: {
    textAlign: "center",
    fontSize: 18,
    color: "gray",
  },
});

export { styles };
